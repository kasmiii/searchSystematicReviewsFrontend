import { ResponseNormalSearch } from './response-normal-search';

describe('ResponseNormalSearch', () => {
  it('should create an instance', () => {
    expect(new ResponseNormalSearch()).toBeTruthy();
  });
});
